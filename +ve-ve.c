#include<stdio.h>
int main()
{
    int num;
    printf("Enter any number");
    scanf("%d",&num);
    if (num>0)
    printf("entered num is positive");
    else
    printf("entered num is negative");
     
     return 0;
}