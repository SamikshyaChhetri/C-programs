#include<stdio.h>
int main()
{
    int n,i;
    int *p;
    printf("Enter the no.of elements\n");
    scanf("%d",&n);
    p=(int*)malloc(n*sizeof(int));
    printf("Enter the array elements:\n");
    for(i=0;i)
}